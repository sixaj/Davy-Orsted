---
label: Page
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: page_sections
  type: blocks
  template_types:
  - grid
  config:
    min: 
    max: 
  label: Page sections
