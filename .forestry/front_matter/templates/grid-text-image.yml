---
label: Grid text + image
hide_body: false
fields:
- name: header
  type: text
  config:
    required: false
  label: Header
- name: text_area
  type: text
  config:
    required: false
  label: text area
- name: image
  type: file
  config:
    maxSize: 64
  label: image
