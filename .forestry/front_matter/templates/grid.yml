---
label: GRID
hide_body: true
fields:
- name: grid
  type: field_group_list
  fields:
  - name: headline
    type: text
    config:
      required: false
    label: Headline
  - name: textfield
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Textfield
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  config:
    min: 
    max: 
    labelField: 
  label: GRID
